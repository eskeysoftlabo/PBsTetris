# PBsTetris

PS5向けESOアドオン。UIは日本語。DDS画像は必ず `.addon` と `.txt` の両マニフェストへ記載する。ファイルをZIPへ入れるだけでは不十分。これはPBsJankenのPS5実機で確認済みの不具合から得たルール。

`## APIVersion` は稼働中の全APIバージョンを並べて記載する（現在は `101050 101051`）。古い方だけを書くとクライアントが期限切れ扱いにしてアドオン自体を読み込まず、「何も起きない」という形で現れる。他のPBs系アドオンはすべて両方を記載している。

クライアントの定数・関数は名前で直接参照する。`rawget(_G,"名前")` で読むと、定数が素の大域変数として置かれていない場合に黙って `nil` になり、機能が無言で止まる。ゲーム本体のコードも `_G[name]` を使い、rawgetは使っていない。

LibGroupBroadcastのプロトコルIDは、プレイヤーが導入しているアドオン全体で共有される番号で、後から宣言した側が `DeclareProtocol` で拒否され、そのアドオンの通信は一切動かない。PBsシリーズでは 510=Translate、511=Janken。Tetrisは510でTranslateと、509でも他のアドオンと重複したため、1.14.0で対戦ごと通信を休止し、現在はプロトコルを宣言していない（`scripts/package.py` が `DeclareProtocol` を検出すると止まる）。新しいアドオンに番号を与えるときは、公式の管理表と導入済みアドオンの両方で確認する。コンソールの読み込み順は名前順ではないため、衝突していても負ける側がクライアントごとに変わり、片方だけ動かないという形で現れる。

配布前に scripts/package.py --check と tests/run.lua を実行する。ゲームパッドの入力レイヤーを画面外に残さない。コンソール実機未検証の点を明示する。対戦を再開する場合の手順はREADMEの「対戦機能（休止中）」にある。
